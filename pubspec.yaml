name: stars
description: A flutter app to invest time to setup architecture basement
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  auto_route: ^5.0.4
  built_value: ^8.6.0
  cached_network_image: ^3.2.3
  connectivity_plus: ^4.0.1
  dio: ^5.1.2
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_redux: ^0.10.0
  intl: ^0.18.0
  json_annotation: ^4.8.1
  redux_epics: ^0.15.1

dev_dependencies:
  auto_route_generator: ^5.0.3
  build_runner: ^2.4.4
  built_value_generator: ^8.6.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  integration_test:
    sdk: flutter
  json_serializable: ^6.7.0
  mocktail: ^0.3.0

flutter:
  uses-material-design: true
  generate: true
